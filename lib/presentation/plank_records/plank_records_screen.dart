import 'package:flutter/material.dart';

class PlankRecordScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child : Text('This is record screens'),
      ),
    );
  }
}