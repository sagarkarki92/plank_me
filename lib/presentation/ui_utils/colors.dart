import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF404e7c);
  static const primaryDark = Color(0xFF251f47);
  static final primaryLight = primary.withOpacity(0.3);

  static const accent = Color(0xFFFE7F2D);
  static const error = Color(0xFF8F0000);

  static const light = Color(0xFFFFFFFF);
  static const lightAccent = Color(0xFFfefeff);
  static const background = Color(0xFFf0f3f5);

  static const dark = Color(0xFF151515);
  static const darkAccent = Color(0xFF353535);
  static const darkSurface = Color(0xFF1f2633);
  static final darkFaded = dark.withOpacity(0.6);
}
